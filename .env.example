# IQRadar 环境配置示例：复制为 .env 后按需修改；真实密钥不要提交进 git。
# 英文占位符 sk-your-gateway-api-key 请替换为你自己网关的 key。
GATEWAY_BASE_URL=http://localhost:8080/v1
GATEWAY_MODEL_NAME=gateway/deepseek-v4-flash
# Set only when the local gateway requires auth.
GATEWAY_API_KEY=sk-your-gateway-api-key
# 以上网关地址/密钥也可以直接在测试页「网关配置」卡片里填写（保存到
# data/settings/gateway.json，立即生效、无需改代码）；页面里的配置优先于本文件。
# 待测模型名称列表（逗号/空格分隔）。设置后测试页“待测模型”以此为准；
# 为空/不设置时自动从网关 /v1/models 发现（需 GATEWAY_BASE_URL）；
# 网关不可达再回退到 configs/models.yaml。
IQRADAR_MODEL_NAMES=gateway/deepseek-v4-flash,gateway/example-35b,z.ai/glm-5.2,gateway/example-236b,minimax/minimax-m2.7,qwen/qwen3.6-35b,moonshotai/kimi-k2.6
# Path to the DeepSWE benchmark directory relative to the project root.
# It must contain `tasks/` and a `.env` with OPENAI_BASE_URL/OPENAI_API_KEY.
# The default points at the sibling deep-swe checkout used by the CLI flow.
# IQRADAR_DEEPSWE_DIR=checkouts/deep-swe
